global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'task-service'
    # Так как Prometheus в Docker, а твой сервис локально, используем host.docker.internal
    static_configs:
      - targets: ['host.docker.internal:8080']